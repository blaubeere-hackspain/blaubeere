# Archived receipt forecast

Retired on 20 September 2026 at the user’s request. Nothing in this directory is imported, compiled, served or deployed by the active application.

The original repository layout is preserved below this directory: selected JSON estimators and monthly profiles, Python runtime/contracts/builder, training and report tools, tests, Rust inference source and deployment notes. Model and scientific artifact bytes are unchanged; their manifests and hashes still describe the original paths.

These are reference sources, not current run or deployment instructions. They depend on the matching historical repository context (including shared financial-evidence helpers and sealed data). To reproduce the former environment, use a separate worktree at `0684d85`, before retirement. Do not rerun sealed training or evaluation phases.

The active v4 health-score model, recorded cash flows, payment arrears, currency-risk layer and deterministic cash-planning engine are separate and remain in the application.
