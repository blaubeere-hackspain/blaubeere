import { appOrigin, pageMetadata } from "../../../metadata";

export const metadata = pageMetadata(appOrigin, "/dashboard", "Cash outlook | blau", "Explore your company’s cash outlook, understand upcoming shortfalls and compare conditional plans in your private blau workspace.");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
