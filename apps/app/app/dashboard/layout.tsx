import { appOrigin, pageMetadata } from "../../../metadata";

export const metadata = pageMetadata(appOrigin, "/dashboard", "Financial health | blau", "Review your company’s published health history, cash movements, payment timing, debt service and source evidence in your private blau workspace.");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
