import { appOrigin, pageMetadata } from "../../../metadata";

export const metadata = pageMetadata(appOrigin, "/connect", "Connect your assistant | blau", "Review and approve an assistant’s access to your authorised company outlooks and planning tools. You stay in control of the connection.");
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
